# FE platform / Nx upgrade: disable per-plugin isolation to reduce overhead (local + CI).
NX_ISOLATE_PLUGINS=false
# FE platform / Nx upgrade: native command runner breaks Jest interactive mode / normal stdout+stderr; use classic runner for tests.
NX_NATIVE_COMMAND_RUNNER=false
